pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)  // 改为 PREFER_PROJECT
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "AndroidUI"
include ':app'